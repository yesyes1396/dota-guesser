# Dota Guesser

Угадайте героя Dota 2 за минимальное количество попыток! 🎮

## Описание

Dota Guesser — это интерактивная веб-игра, где вы должны угадать случайно выбранного героя Dota 2. Система подсказок показывает совпадения по полу, типам, ролям, атрибуту, типу атаки, сложности и году выпуска.

## Возможности

- 🎯 **127+ героев** в базе данных
- 💡 **Подсказки** — зелёные pills показывают совпадения
- 🖼️ **Иконки героев** с fallback цепочкой (PNG → SVG → CDN)
- ✏️ **Редактор героев** — добавляйте и изменяйте данные
- 🔍 **Автодополнение** — поиск с подсказками при вводе
- 💾 **Сохранение** — данные хранятся в localStorage
- 📸 **Вью всех героев** — смотрите полный список
- 🏳️ **Сдача** — кнопка для того, чтобы посмотреть ответ

## Установка и запуск

### Локально

```bash
# Клонируйте репозиторий
git clone https://github.com/YOUR-USERNAME/dota-guesser.git
cd dota-guesser

# Запустите локальный сервер
python -m http.server 5000

# Откройте браузер
# http://localhost:5000
```

### На GitHub Pages

Проект полностью статический и может быть размещён на [GitHub Pages](https://pages.github.com/):

1. Откройте Settings репозитория
2. Перейдите в **Pages**
3. Выберите ветку `main` и папку `/root`
4. Сохраните

Ваш сайт будет доступен по адресу: `https://YOUR-USERNAME.github.io/dota-guesser`

## Структура проекта

```
dota-guesser/
├── index.html          # Главная страница
├── app.js              # Логика игры
├── styles.css          # Стили
├── server.py           # Опциональный сервер
├── heroes.json         # База данных героев
├── assets/
│   ├── icons/          # PNG иконки героев (127)
│   └── allheroes.png   # Превью всех героев
└── .gitignore          # Исключения Git
```

## Как играть

1. **Введите имя героя** (на английском, e.g. "Anti-Mage")
2. **Смотрите подсказки** — зелёные поля значат совпадение
3. **Угадайте героя** или нажмите "Сдаться"
4. **Новая игра** — нажмите кнопку "Новая игра"

## Опции сервера

Для использования функции сохранения на сервер:

```bash
python server.py
```

Сервер слушает на `localhost:5000` и имеет endpoint `/save_heroes` для POST запросов.

## Данные героев

`heroes.json` содержит:
- Имя героя
- Пол
- Типы (видео, анимация)
- Роли (Carry, Support и т.д.)
- Атрибут (STR, AGI, INT, UNI)
- Тип атаки (Melee, Ranged)
- Сложность
- Год выпуска
- Внутреннее имя файла иконки

##Автор
https://github.com/yesyes1396
